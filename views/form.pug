extends layout

block content
  main
    section(class="form-section")
      form(class="form" method="POST" action="/new")
        input(class="input-text" type="text", name="user" placeholder="Your name")
        input(class="input-text" type="text", name="message" placeholder="Say something")
        button(class="submit-button" type="submit") Submit
    section(class="nav")
      a(href="http://localhost:3000/"): button(type="button") Go Back
      